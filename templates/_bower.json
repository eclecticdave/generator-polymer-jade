{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.1",
  "dependencies": {<% if (cssProcessor === 'sass') { %>
    "sass-bootstrap": "~3.0.0",<% } %>
    "platform": "Polymer/platform#master",
    "polymer": "Polymer/polymer#master"<% if (uiElements) { %>,
    "polymer-ui-elements": "master"<% } %>
  },
  "devDependencies": {}
}
